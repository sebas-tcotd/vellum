# Dependency directories
node_modules/
.target/

# Build outputs
dist/
dist-node/
.turbo/
build/
.next/
.cache/
src-tauri/target/
.pnpm-store/

# Rust / Tauri artifacts
target/
*.tsbuildinfo
*.msi
*.dmg
*.AppImage

# BMAD / AI runner artifacts
_bmad/
_bmad-output/
.agent
.agents
.claude
.cursor
.gemini
.windsurf
.roo
.opencode
.codex
.augment/
.clinerules
.mcp.json
CLAUDE.local.md
AGENTS.md
CLAUDE.md
opencode.json
# Test output folders
**/test-results/

# Capturas de Vellum Bridge (.cslmap + Raw Snapshot): pesan cientos de MB, se
# comparten entre máquinas por fuera de Git. El manifest las marca storage: local.
research/vellum-bridge/snapshots/
research/vellum-bridge/cslmap/

# 6.2A golden harness decodes .png on demand (see harness.mjs); a persisted
# .rgba sidecar is ~10x the size of its .png and nothing reads it.
packages/renderer-webgl/test/export-goldens/**/*.rgba

# OS files
.DS_Store
*.swp

# Environment files
.env
.env.local
# Captures the golden-flow visual comparison writes next to a failing baseline
# for inspection. Scratch output, never evidence — see the baselines README.
apps/desktop/tests/e2e/baselines/*.actual.png

# Build from .NET
tools/vellum-bridge/bin
tools/vellum-bridge/obj
tools/vellum-bridge/tests/bin
tools/vellum-bridge/tests/obj

