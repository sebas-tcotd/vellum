[package]
name = "parser-cslmap"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
quick-xml = { version = "0.36" }
tauri = { version = "2", features = [] }

[dev-dependencies]
serde_json = "1"
